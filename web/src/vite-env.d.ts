/// <reference types="vite/client" />

// Required, not boilerplate: tsconfig.app.json sets
// noUncheckedSideEffectImports, so `import "@/index.css"` is an error
// without the ambient module declarations Vite ships here.
