# Core dependencies -- used throughout src/ and optimization_controller.py
# since the project's original files.
# Floors bumped to the latest mutually-compatible versions as of 2026-08-19,
# verified together: full test suite (700 tests) + ruff check/format all
# pass against this exact combination in a clean venv.
pandas>=3.0.5
numpy>=2.5.2

# Required for BacktestConfig.from_yaml (Task 6.1).
pyyaml>=6.0.3

# Optional: only required for search_strategy="bayesian" (Task 5.3).
# search_strategy="grid" (the default) needs none of this. Without it,
# BayesianSearch(...) raises a clear ConfigurationError rather than
# failing at import time or silently falling back to grid search.
optuna>=4.9.0

# Optional: only required for live trading against Alpaca (Phase 7).
# Backtesting needs none of this. src/fill_accounting.py's core
# arithmetic is SDK-independent; only extract_alpaca_fill() and the
# live broker path touch alpaca-py types.
alpaca-py>=0.44.0

# NOT HERE: the Fidelity live-trading dependencies. They live in
# requirements-fidelity.txt instead, deliberately -- Dockerfile does
# `pip install -r requirements.txt` for every image, and Playwright
# would pull a browser-automation stack into containers that only run
# backtests, while still being unusable there without a separate
# `playwright install firefox` for the browser binary. Optuna and
# alpaca-py stay in this file because they are light and pure-Python;
# Playwright is a different class of dependency.

# Development / test only.
pytest>=9.1.1

# Formatting and linting. Configured in pyproject.toml.
#   ruff format .        -- apply formatting
#   ruff format --check . -- verify formatting without writing (CI)
#   ruff check .          -- lint
#   ruff check --fix .    -- lint and apply safe fixes
ruff>=0.16.3

# Optional: only required for the read-only live dashboard
# (`streamlit run dashboard.py`). Backtesting and live trading need none
# of this -- src/dashboard_data.py imports no streamlit at all, and its
# tests skip when the package is absent, so an install without this is a
# fully working deployment minus one window onto it.
streamlit>=1.58.0
