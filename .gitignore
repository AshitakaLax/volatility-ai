__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/
.venv/
venv/
.env
.env.*

# Created locally by the docker-compose.yml bind mounts (state/, data/,
# output/) or by running the SQLite-backed store directly -- never
# meant to be committed. Mirrors .dockerignore's own exclusions.
state/
output/
*.db
